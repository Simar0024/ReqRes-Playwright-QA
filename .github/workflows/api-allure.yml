name: Automated API Quality Gate

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  api-testing-matrix:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Codebase
      uses: actions/checkout@v4

    - name: Setup Node.js Runtime
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install Application Dependencies
      run: npm install

    # ------------------------------------------------------------------------
    # EXECUTION STAGE 1: RUN LEGACY REQRES TESTS
    # ------------------------------------------------------------------------
    - name: Execute Legacy ReqRes Automated Tests
      env:
        REQRES_API_KEY: ${{ secrets.REQRES_API_KEY }}
      run: npm run test:legacy
      continue-on-error: true

    # ------------------------------------------------------------------------
    # EXECUTION STAGE 2: RUN REAL-SETUP MICROSERVICE CLUSTER
    # ------------------------------------------------------------------------
    - name: Execute Real-Setup Production Cluster Tests
      run: npm run test:real-setup
      continue-on-error: true

    # ------------------------------------------------------------------------
    # REPORT ARCHITECTURE INTEGRATION STAGE
    # ------------------------------------------------------------------------
    - name: Load Historical Test Metrics
      uses: actions/checkout@v4
      if: always()
      continue-on-error: true
      with:
        ref: gh-pages
        path: gh-pages-dir

    # Compile Part A into its own isolated temporary workspace directory
    - name: Map Legacy Allure Structural Package
      uses: simple-elf/allure-report-action@master
      if: always()
      id: allure-legacy
      with:
        allure_results: allure-results
        gh_pages: gh-pages-dir
        allure_report: allure-report-legacy
        allure_history: allure-history-legacy
        subfolder: legacy

    # Compile Part B into its own isolated temporary workspace directory
    - name: Map Real-Setup Allure Structural Package
      uses: simple-elf/allure-report-action@master
      if: always()
      id: allure-real
      with:
        allure_results: real-setup/allure-results
        gh_pages: gh-pages-dir
        allure_report: allure-report-real
        allure_history: allure-history-real
        subfolder: real-setup

    # ------------------------------------------------------------------------
    # FIXED UNIFIED ASSEMBLER LAYER (Corrected local path resolution)
    # ------------------------------------------------------------------------
    - name: Consolidate Subfolders & Generate Navigation Router
      if: always()
      run: |
        mkdir -p final-allure-payload/legacy
        mkdir -p final-allure-payload/real-setup
        
        # 🛠️ FIX: Copy files directly from the root of the generated build outputs
        if [ -d "allure-report-legacy" ]; then
          cp -r allure-report-legacy/* final-allure-payload/legacy/
        fi
        
        if [ -d "allure-report-real" ]; then
          cp -r allure-report-real/* final-allure-payload/real-setup/
        fi
        
        # Clean up accidental recursive folders if the action duplicated them
        rm -rf final-allure-payload/legacy/legacy
        rm -rf final-allure-payload/real-setup/real-setup
        
        # Inject the Master Hub HTML router directly into the root layout folder
        cat << 'EOF' > final-allure-payload/index.html
        <!DOCTYPE html>
        <html>
        <head>
            <title>QA Quality Gate Portal</title>
            <style>
                body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; text-align: center; padding: 50px; background: #f8f9fa; }
                .container { max-width: 600px; margin: 0 auto; background: white; padding: 40px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.05); }
                h1 { color: #202124; margin-bottom: 30px; }
                .btn { display: block; margin: 15px auto; padding: 15px 25px; background: #1a73e8; color: white; text-decoration: none; border-radius: 8px; font-weight: bold; width: 60%; transition: background 0.2s; }
                .btn:hover { background: #1557b0; }
                .btn-secondary { background: #34a853; }
                .btn-secondary:hover { background: #24753a; }
            </style>
        </head>
        <body>
            <div class="container">
                <h1>Infrastructure Verification Hub</h1>
                <a href="./legacy/" class="btn">📊 View Legacy ReqRes API Dashboard</a>
                <a href="./real-setup/" class="btn btn-secondary">🚀 View Real-Setup Cluster Dashboard</a>
            </div>
        </body>
        </html>
        EOF
        
        # Standard bypass flag for GitHub Pages Jekyll processing engines
        touch final-allure-payload/.nojekyll

    # ------------------------------------------------------------------------
    # BACKUP AND HOSTING DEPLOYMENT OPERATIONS
    # ------------------------------------------------------------------------
    - name: Backup Entire Consolidated Payload to gh-pages Branch
      uses: peaceiris/actions-gh-pages@v4
      if: always()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_branch: gh-pages
        publish_dir: final-allure-payload

    # Setup the hosting infrastructure variables using the consolidated payload directly
    - name: Initialize Pages Static Hosting Layer
      uses: actions/configure-pages@v5
      if: always()

    # Stage the clean consolidated folder to the GitHub Actions upload bundle
    - name: Stage Manifest Tarball Package to Hosting Engine
      uses: actions/upload-pages-artifact@v3
      if: always()
      with:
        path: 'final-allure-payload'

    # Deploy everything instantly to the web
    - name: Publish Consolidated Allure System Report Dashboard
      id: deployment
      uses: actions/deploy-pages@v4
      if: always()